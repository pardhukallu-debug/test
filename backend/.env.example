# Server Configuration
PORT=8000
HOST=0.0.0.0

# CORS Allowed Origins (comma-separated, or * for all)
ALLOWED_ORIGINS=*

# Optional: Google Maps API Key (if omitted, falls back to free OpenStreetMap / OSRM)
# GOOGLE_MAPS_API_KEY=your_key_here
